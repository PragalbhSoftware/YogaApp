@model YogaMarketplace.Web.Pages.Instructor.SlotMutationBody
<div data-mutation data-ok="@(Model.Ok ? "true" : "false")" data-stays="true">
    @if (!string.IsNullOrEmpty(Model.Notice))
    {
        <div class="notice notice-ok" role="status" data-notice>
            <p>@Model.Notice</p>
        </div>
    }
    else if (!string.IsNullOrEmpty(Model.Error))
    {
        <div class="notice notice-alert" role="alert" data-notice>
            <p>@Model.Error</p>
        </div>
    }
    @if (Model.Card is not null)
    {
        <partial name="_SlotCard" model="Model.Card" />
    }
</div>
