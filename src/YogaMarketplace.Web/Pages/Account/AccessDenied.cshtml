@page "/account/access-denied"
@{
    ViewData["Title"] = UiCopy.AccessDeniedTitle;
}

<h1>@UiCopy.AccessDeniedTitle</h1>
<p class="lead">@UiCopy.AccessDeniedLead</p>

@if (User.IsInRole(AccountRoles.Admin))
{
    <a class="button" asp-page="/Admin/Index">@UiCopy.OpenAdmin</a>
}
else if (User.IsInRole(AccountRoles.Provider))
{
    <a class="button" asp-page="/Instructor/Bookings">@UiCopy.InstructorInboxNav</a>
    <a class="button secondary" asp-page="/Instructor/Availability">@UiCopy.InstructorAvailabilityNav</a>
    <a class="button secondary" asp-page="/Instructor/Register">@UiCopy.InstructorStatusNav</a>
}
else if (User.IsInRole(AccountRoles.Customer))
{
    <a class="button" asp-page="/Bookings/Index">@UiCopy.BookingsNav</a>
    <a class="button secondary" asp-page="/Instructor/Register">@UiCopy.BecomeInstructor</a>
}
else
{
    <a class="button" asp-page="/Account/SignIn">@UiCopy.SignInCta</a>
}
