@page
@model YogaMarketplace.Web.Pages.ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1>Something went wrong</h1>
<p class="lead">Try that again. If it keeps happening, come back in a moment.</p>
@if (Model.ShowRequestId)
{
    <p class="muted">Reference @Model.RequestId</p>
}
