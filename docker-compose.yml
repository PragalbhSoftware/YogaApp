services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "YogaDev!Passw0rd"
    ports:
      - "1433:1433"
    volumes:
      - yoga-sql:/var/opt/mssql

volumes:
  yoga-sql:
